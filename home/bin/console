#!/usr/bin/env bash
## Console Script

motd() { 
  templ banner "Local Environment" && env
  templ banner "Network Connections" && lsof -i -P -n | grep LISTEN
  templ banner "$(date)\n  Running $(uname -n) on $(uname -o) $(uname -m) platform."
}

## Run motd.
motd

## Open console.
bash
